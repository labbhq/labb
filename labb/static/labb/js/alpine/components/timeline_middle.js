// timeline_middle.js

const timelineMiddleConfig = {
  baseClasses: ["timeline-middle"],
  variables: {
  }
};

window.lb.createComponent(timelineMiddleConfig, 'lbTimelineMiddleComp', 'timeline.middle');
