// mockup_phone_camera.js

const mockupPhoneCameraConfig = {
  baseClasses: ["mockup-phone-camera"],
  variables: {
  }
};

window.lb.createComponent(mockupPhoneCameraConfig, 'lbMockupPhoneCameraComp', 'mockup-phone.camera');
